const INITIAL_STATE = {};

const keyboardReducers = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    case "TYPE":
      break;

    default:
      return state;
  }
};

export default keyboardReducers;
